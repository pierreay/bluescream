;;; Directory Local Variables
;;; For more information see (info "(emacs) Directory Variables")

((nil . ((compile-command . (format "make -f %s push" (get-closest-pathname "Makefile.emacs")))
         (compilation-read-command . nil))))
